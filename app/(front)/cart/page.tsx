import React from "react";
import CartDetails from "./CartDetails";

export const metadata = {
  title: "Cart",
  description: "Cart",
};
const page = () => {
  return <CartDetails />;
};

export default page;
